/* ============================================================
   Companio — shared runtime config
   ============================================================
   THIS IS THE ONLY FILE YOU EDIT WITH YOUR KEYS.
   Paste your Supabase project URL + anon (public) key below ONCE.
   Every page (dashboard, both portals, set-password) loads this file,
   so you never paste keys into the HTML again.

   · This file is NOT regenerated by builds — safe across re-deploys.
   · Use the anon / public key only (NEVER the service_role key here).
   · Leave both blank to run the dashboard on demo data (no login).
   ============================================================ */

// ----- EDIT THESE TWO LINES (once) -----
window.SUPABASE_URL      = "https://bouyfsfcfjeordmaaaof.supabase.co";   // e.g. https://bouyfsfcfjeordmaaaof.supabase.co
window.SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJvdXlmc2ZjZmplb3JkbWFhYW9mIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODE3MjE5MzMsImV4cCI6MjA5NzI5NzkzM30.Y6KjAafCqTSX_njw4XDJZzv7Ccd6uIDbZW8TCU1JELI";   // your anon / public key (starts with eyJ...)
// ---------------------------------------

/* Back-compat: existing scripts read bare globals `SUPABASE_URL` /
   `SUPABASE_ANON_KEY` (via `typeof ... !== 'undefined'`). Because this file
   loads at top-level before supabase.js, these `var` declarations create
   those globals. Do not remove. */
var SUPABASE_URL = window.SUPABASE_URL;
var SUPABASE_ANON_KEY = window.SUPABASE_ANON_KEY;
